
function signupGoogle() {
    console.log("Google sign up button was clicked");

    // Set up URL for Google auth endpoint including all the CSRF goodness
}

// Add event listener on sign up button
document.getElementById('button_google_sign_up').addEventListener('click', signupGoogle);
